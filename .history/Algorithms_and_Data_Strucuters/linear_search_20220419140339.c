#include <stdio.h>
int LinearSearch(int Arr[], int size);
int main()
{
    int Arr[4] = {2, 4, 6, 8};
    return 0;
}
int LinearSearch(int Arr[], int size,int )
{
    int iter;
    for (iter = 0; iter < size; iter++){
        if (Arr[iter] == )
    }
}
